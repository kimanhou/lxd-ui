import type { FC } from "react";
import { Card } from "@canonical/react-components";

const MemoryCard: FC = () => {
  return <Card className="overview-card memory" title="Memory"></Card>;
};

export default MemoryCard;
